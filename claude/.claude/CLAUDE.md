# Global Instructions

## Git Safety
- NEVER push directly to main/master. Always create a branch and open a PR.
- NEVER merge a PR. The user will merge PRs themselves.
- ALWAYS run `git branch` to verify the current branch before committing or pushing. The user may have switched branches between turns.
- When asked to address PR comments, please respond to the comments as well.
